# 导购前端

React + TypeScript + Vite 编辑器，零一数字人导购平台的 Web UI。

## 启动

```bash
cd guide
npm install
npm run dev:web   # http://127.0.0.1:5173
```

## 脚本

- `npm run dev` — 同时启动 server + web
- `npm run build` — 生产构建
- `npm run lint` — ESLint 检查
- `npm run test:e2e` — Playwright E2E 测试
