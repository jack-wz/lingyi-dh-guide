# Local brand system assets

Font binaries are **not** committed (see repo `.gitignore`). Import via:

```bash
cd guide
npm run dev:server   # once, to seed from local-brand-system
```

Or copy your brand fonts under `fonts/<FamilyName>/` matching `local-brand-system.ts` paths.

Markdown design tokens live under `default/` when present.